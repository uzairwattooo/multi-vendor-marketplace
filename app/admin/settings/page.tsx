import AdminModulePlaceholder from "@/components/admin/AdminModulePlaceholder";

export default function Page() {
    return (
        <AdminModulePlaceholder
            eyebrow="Marketplace configuration"
            title="Settings"
            description="Centralize commission, payment, order and marketplace configuration."
            nextStep="Next: marketplace settings schema, commission controls and payment toggles."
        />
    );
}
