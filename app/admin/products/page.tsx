import AdminModulePlaceholder from "@/components/admin/AdminModulePlaceholder";

export default function Page() {
    return (
        <AdminModulePlaceholder
            eyebrow="Catalog moderation"
            title="Products"
            description="Review every marketplace product, stock level, status and featured placement."
            nextStep="Next: product table, store filters, feature toggle and archive actions."
        />
    );
}
