import AdminModulePlaceholder from "@/components/admin/AdminModulePlaceholder";

export default function Page() {
    return (
        <AdminModulePlaceholder
            eyebrow="Order operations"
            title="Orders"
            description="Monitor every buyer order and seller fulfilment status from one place."
            nextStep="Next: order filters, detail page, timeline and controlled status override."
        />
    );
}
